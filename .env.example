# This is an example environment file
# Copy this to .env in both client/ and server/ directories and fill in your values

# See client/.env.example and server/.env.example for specific configurations
